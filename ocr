#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
[[ -x "$ROOT/.venv/bin/python" ]] || { echo "Run $ROOT/setup.sh first." >&2; exit 1; }
exec "$ROOT/.venv/bin/python" "$ROOT/app.py" "$@"
